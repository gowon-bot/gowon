export const toggleValues = {
  ON: "on",
  OFF: "off",
};

export enum FMMode {
  DEFAULT = "default",
  VERBOSE = "verbose",
  COMPACT = "compact",
  ALBUM = "album",
  COMBO = "combo",
  CUSTOM = "custom",
}
