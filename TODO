Bugs:
  [x] Yoinking still bugged
  [] `!albumat` out of bounds (probably aa and tra too)
  [] `!o all` help
  [] infinity in `!o per`
  [] need to be logged in to run commands
  [] Number display in `!o all`

Test:
  [] Steal crown from signed out/gone user

Todo:
  [] artist correct method
  [] Change rank command to be like !tt
  [] Add top artists to tags
  [] Add total tracks/albums/scrobbles to att, ltt, att, last
  [later] Dockerize 😳
  [later] Keep track of every tag, artist ever fm'd

  Comands:
    [] Crowns ban
    [later] Whosampled (w/scraping)

    
Dashboard:
  [] Make TrackedErrors individual instead of grouped
      
Large Projects:
  [] Change lastfm service methods to take specific object type params
  [] Change User.username into GuildMember.nickname (or w/e it's called)
  [] Feedback command to automatically make gh issue
  [] Permission caching
  [] !flashplaylist with spotify integration
  [] `!crowns mark 수호 duplicate of suho`
  [] Cache redirects
  [] Spotify integration
  [] Crowns history

