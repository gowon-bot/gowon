
Todo:
  [] Keep track of every command run
  [x] Make AliasChecker case insensitive

  Permissions:
    [x] Display friendly names instead of IDs
    [x] Fix blacklisting
    [x] Fix whitelisting  
    [x] Allow viewing permissions
    [x] Enable permissions for subcommands
    [] Allow setting permissions for roles without mentioning the role
    [] Allow setting permissions without mentioning the user
    [x] Make permissions unique
    [] Allow white & blacklisting for parentcommands
    [] Allow listing of disabled commands