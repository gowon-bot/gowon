Bugs:
  [] `.cw mb 수호 |suho` => `@undefined, a crown for uho doesn't exist!`
  [] Redirects for crowns broken
  
Test:  

Todo:
  [] Add scrobbles to `!o top50`
  [later] Dockerize 😳
  [later] Keep track of every tag, artist ever fm'd

  Comands:
    [later] Whosampled (w/scraping)

    
Dashboard:
  [] Make TrackedErrors individual instead of grouped
      
Large Projects:
  [x] `!crowns mark 수호 duplicate of suho`
  [x] Crowns history

  [] Redirect verification (by scrobbling both artists, then adding one scrobble to the first artist, and checking if the second has updated with it)
  [] Automatic argument validation
  [] Tests
  [] Change User.username into GuildMember.nickname (or w/e it's called)
  [] Feedback command to automatically make gh issue
  [] Permission caching
  [] Spotify integrations
  [] !flashplaylist with spotify integration

